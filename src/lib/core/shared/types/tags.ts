/**
 * Shared Tag Types
 * Common tag type definitions used across frontend and backend
 */

/**
 * Tag categories for classification
 */
export type TagCategory = 'feature' | 'theme' | 'mechanic' | 'style';

/**
 * Tag source types - where tags can be applied
 */
export type TagSource = 'games' | 'providers' | 'tournaments' | 'promotions' | 'all';