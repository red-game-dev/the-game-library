/**
 * useProviders hook
 * Simple wrapper around useProvidersQuery
 */

import { useProvidersQuery as useQuery } from '@/lib/core/frontend/api/providers/hooks/useProvidersQuery';

export const useProvidersQuery = useQuery;